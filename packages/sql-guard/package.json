{
  "name": "@neetru/sql-guard",
  "version": "0.1.6",
  "description": "Classificador read-only de SQL + gate de hardening do Neetru Core DB. Decide se um statement é seguro para o visualizador de banco, impõe LIMIT em SELECTs e barra break-glass com WHERE tautológico. Fail-closed.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "node-sql-parser": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
